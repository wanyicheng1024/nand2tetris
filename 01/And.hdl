/**
 * And gate: 
 * out = 1 if (a == 1 and b == 1)
 *       0 otherwise
 * And = Not Nand
 */

CHIP And {
    IN a, b;
    OUT out;

    PARTS:
    // Put your code here:
    Nand(a=a, b=b, out=o1);
    Not(in=o1, out=out);
}
